TODO (first release)
- clean up the code
-- look for TODOs
-- look for missing documentation 
- Full english documentation
- Add SeeAlso::DBIServer->import
- Reinstall existing servers (and remove "new" from Source::DBI then!)
- Add Wikipedia extractor as example (ISBN2Wikipedia should be enough)
- Somehow point to the JavaScript clients
- add test of SeeAlso::DBISource with SQLite

TODO (later releases)
- Add SeeAlso::Test for testing SeeAlso services 
-- Create a management object (needed for testing and quality of service)
- Add logging and better error handling
-- Statistics/Logging-Source-Pipe
- Better support of OpenSearch Description (more fields)
- Add SeeAlso::Discovery to discover servers via OpenSearch Description 
- Support more of the OpenSearch Description fields
- Run as a real server (on a port of your choice) : better performance?
- Add SeeAlso::Source::Union
- Add SeeAlso::Source::Cache
- Add XSLT to OpenSearch Description
- Add an example of a multilangual source with equal identifiers: LibraryThing
