This directory contains some examples of services based on the SeeAlso-Module.

You can directly run the scripts if SeeAlso is installed in your PERL library 
path (@INC) or if the 'lib' directory of SeeAlso is a subdirectory of this
example directory (on Unix just create a symlink with "ln -s ../lib/ lib").


cgi-test.pl
    simple CGI SeeAlso webservice for testing

yahoo.pl
    simple wrapper for Yahoo Search Suggest


The following examples provide scripts to extract, transform, and load (ETL)
link data into a SeeAlso::Source::DBI database. The ETL-complex may make it
look more complex than it actually is.


download-thingISBN.sh
    bash script to download thingISBN data

extract-thingISBN.pl
    extract ISBN to LibraryThing mappings from thingISBN data


download-wikipedia.sh
    bash script to download a Wikipedia database dump

extract-mediawiki-templates.pl
    extract templates, ISBN, RFC from MediaWiki article dumps

transform-mediawiki-isbn.sh
    filter out ISBN to Wikipedia mappings from extract-mediawiki-templates.pl

extract-mediawiki-isbn.sh
    extract ISBN to Wikipedia article mappings from a Wikipedia dump


load-mysql.pl
    load extracted SeeAlso mapping data into a MySQL database

